import styled from '@emotion/styled';
import { normalColor } from '../../styles/colors';

export const Container = styled.div({
  width: '30rem',
  padding: '1rem 1rem',
});

export const AnswerBox = styled.div({
  fontSize: '1.4rem',
  height: '50vh',
  backgroundColor: '#1792CC',
  color: normalColor,
  border: '2px solid white',
  borderRadius: '4px',
});
